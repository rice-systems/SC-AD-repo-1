To obtain the modified darknet benchmark suite, use the following commands:
```
git clone https://github.com/AlexeyAB/darknet
cd darknet
git checkout b8dceb7
```

Then apply the uvmdiscard.diff to this commit.

